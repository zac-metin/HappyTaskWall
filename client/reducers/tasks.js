const initialState = [{“id”:1,“taskName”:“create API”,“description”:“set up an API for our project to use”,“user_id”:1,“completionStatus”:0}]

function tasks (state = , action) {
  switch (action.type) {
    default:
      return state
  }
}

export default subreddits
